//! Concrete substrate implementations.
//!
//! Each module here implements the [`Substrate`](crate::ports::Substrate)
//! port (plus the component control ports) for a real infrastructure target.

pub mod docker;
